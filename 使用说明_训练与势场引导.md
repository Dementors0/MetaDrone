python preview_map.py --seed 123 --output-html preview_map.html
# 多无人机训练使用说明（A* 与 Dijkstra 势场）

本文档基于当前代码版本，给出可直接执行的使用方法。你将看到：

- 如何使用 conda 环境 DiffPhysDrone
- 如何预构建势场地图
- 如何在 A* 与 Dijkstra 势场之间切换
- 如何使用 U 型陷阱与墙体物理反馈开关
- 如何查看势场模式下的 HTML 与日志


## 1. 关键文件

- 训练主入口: mmgj_transformer.py
- 轻量训练入口: main_cuda.py
- 多分区环境: env_multi.py
- 势场工具: potential_map_utils.py
- 离线预构建脚本: precompute_potential_maps.py


## 2. 环境准备（conda: DiffPhysDrone）

先激活 conda 环境，再进入项目目录。

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
```

功能示例：验证当前 Python 与环境。

```bash
conda activate DiffPhysDrone
python -c "import sys; print(sys.executable)"
```


## 3. 顶层引导后端切换

通过 guidance_backend 统一切换：

- astar: 在线 A* 引导
- dijkstra_potential: 离线 Dijkstra 势场引导

功能示例 A（A* 后端）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py --guidance_backend astar
```

功能示例 B（势场后端）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps \
  --num_precomputed_maps 100
```


## 4. 预构建势场地图（势场后端必需）

### 4.1 生成 100 张缓存图

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python precompute_potential_maps.py \
  --num_maps 100 \
  --save_dir ../precomputed_maps
```

并行加速（32 核机器推荐示例）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python precompute_potential_maps.py \
  --num_maps 100 \
  --save_dir ../precomputed_maps \
  --num_workers 24 \
  --max_retries 2
```

参数说明：

- --num_workers: 进程数（<=0 自动）
- --chunksize: 自动任务分片粒度（<=0 自动）
- --max_retries: 失败重试轮数

脚本会输出 tqdm 进度条，并把失败样本写入：

- ../precomputed_maps/precompute_failures.log

输出文件示例：

- ../precomputed_maps/map_000.pt
- ...
- ../precomputed_maps/map_099.pt


### 4.2 控制是否包含 U 型陷阱

功能示例 A（包含 U，默认）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python precompute_potential_maps.py \
  --num_maps 100 \
  --save_dir ../precomputed_maps \
  --include_u_local_optimum
```

功能示例 B（不包含 U）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python precompute_potential_maps.py \
  --num_maps 100 \
  --save_dir ../precomputed_maps \
  --no_include_u_local_optimum
```

说明：不包含 U 时，三分区为 hard/easy/easy，并随机调换顺序。


### 4.3 势场 3D 栅格精细度怎么调

下面这组参数决定势场“离散化质量”和“保守程度”：

- --resolution：体素边长（米），越小越精细，路径更贴近几何真实，但构建更慢、内存更大
- --margin：障碍膨胀边距（米），越大越保守，越不容易贴墙
- --z_min / --z_max：势场覆盖高度范围，应覆盖实际飞行高度与目标高度

经验建议：

- 首次跑通：resolution=0.10~0.15，margin=0.10~0.20
- 追求更细导航：将 resolution 降到 0.08 左右，再观察构建时间与日志收益
- 如果轨迹太贴墙：适当增大 margin（例如 +0.05）

功能示例 A（更精细、略慢）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python precompute_potential_maps.py \
  --num_maps 100 \
  --save_dir ../precomputed_maps \
  --resolution 0.04 \
  --margin 0.15 \
  --z_min 0.0 \
  --z_max 5.0
```

功能示例 B（更快、较粗）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python precompute_potential_maps.py \
  --num_maps 100 \
  --save_dir ../precomputed_maps \
  --resolution 0.15 \
  --margin 0.10
```


### 4.4 点落在栅格内部时，方向如何计算

训练时查询势场方向由 --potential_interpolation 控制：

- nearest：先把连续坐标吸附到最近体素，再直接取该体素方向
- trilinear：在点所在体素立方体内，对 8 个角点方向做三线性加权

直观理解：

- nearest 像“查最近格子”，方向会有台阶感，适合快速调试
- trilinear 像“局部平滑插值”，方向连续性更好，更适合训练

推荐：默认使用 trilinear；如果只做定位排障可临时切 nearest。

功能示例（训练时切换插值）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps \
  --num_precomputed_maps 100 \
  --potential_interpolation trilinear
```


## 5. 训练启动（含常用功能示例）

### 5.1 势场后端 + 三线性查询（推荐训练）

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps \
  --num_precomputed_maps 100 \
  --potential_interpolation trilinear
```

### 5.2 势场后端 + nearest 查询（调试）

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps \
  --num_precomputed_maps 100 \
  --potential_interpolation nearest
```

### 5.3 A* 后端基线

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py --guidance_backend astar
```


## 6. 地图切换与 reset 机制

当前机制保持：

- maze_update_interval 控制同图训练轮数（默认 50）
- 同一张地图训练 50 轮后切下一张

功能示例：改为每 20 轮切图。

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps \
  --num_precomputed_maps 100 \
  --maze_update_interval 20
```


## 7. 环境功能开关

### 7.1 U 型局部最优陷阱（训练阶段）

功能示例 A（开启）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py --include_u_local_optimum
```

功能示例 B（关闭）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py --no_include_u_local_optimum
```

### 7.2 墙壁物理反馈

功能示例 A（开启墙体软接触反馈）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py --wall_physical_feedback
```

功能示例 B（关闭，保持自由运动结果）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py --no_wall_physical_feedback
```

### 7.3 两分区紧凑地图开关

功能示例 A（开启两分区紧凑地图）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py --compact_two_zone_map
```

功能示例 B（关闭，恢复当前默认布局）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py --no_compact_two_zone_map
```

开启后效果：

- 地图仅保留 easy/hard 两种分区
- 地图 Y 向范围缩小
- 起点/终点平面随地图自动调整

### 7.4 开关依赖关系速查

以下关系建议一起看：

1. `compact_two_zone_map` 与 `include_u_local_optimum`

- 当 `--compact_two_zone_map` 开启时，布局固定为两分区（easy/hard），`U` 区不会生效。
- 当 `--no_compact_two_zone_map`（默认）时，`include_u_local_optimum` 决定是否包含 U 区。

2. `guidance_backend` 与 `precomputed_map_dir`

- `--guidance_backend dijkstra_potential` 需要可用的预计算地图目录。
- `--guidance_backend astar` 不依赖预计算地图目录。

3. 训练与预计算的布局开关必须一致

- 若训练启用 `--compact_two_zone_map`，建议预计算也启用同开关。
- 若训练关闭 `--compact_two_zone_map`，建议预计算也关闭同开关。

功能示例：预计算与训练同配置（两分区紧凑）

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub

python precompute_potential_maps.py \
  --num_maps 100 \
  --save_dir ../precomputed_maps_compact \
  --compact_two_zone_map \
  --no_include_u_local_optimum

python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps_compact \
  --num_precomputed_maps 100 \
  --compact_two_zone_map
```

### 7.5 开关使用最终版本（建议直接用）

以下是当前版本可直接复制的最终模板。

模板 A：默认布局（三分区逻辑）+ 势场后端

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub

python precompute_potential_maps.py \
  --num_maps 100 \
  --save_dir ../precomputed_maps_default \
  --no_compact_two_zone_map \
  --no_include_u_local_optimum
  --resolution 0.04 \
  --margin 0.15 \
  --z_min 0.0 \
  --z_max 5.0

python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps_default \
  --num_precomputed_maps 100 \
  --no_compact_two_zone_map \
  --no_include_u_local_optimum
```

模板 B：紧凑布局（两分区 easy/hard）+ 势场后端

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub

python precompute_potential_maps.py \
  --num_maps 100 \
  --save_dir ../precomputed_maps_compact \
  --compact_two_zone_map \
  --no_include_u_local_optimum \
  --resolution 0.04 \
  --margin 0.15 \
  --z_min 0.0 \
  --z_max 5.0

python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps_compact \
  --num_precomputed_maps 100 \
  --compact_two_zone_map
```

模板 C：A* 后端（不依赖预计算地图）

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py \
  --guidance_backend astar \
  --compact_two_zone_map
```

最终规则（务必遵守）：

1. `guidance_backend=dijkstra_potential` 时，预计算与训练必须使用同一套 `compact_two_zone_map` 开关。
2. `compact_two_zone_map` 开启时，`include_u_local_optimum` 不再生效（两分区优先）。
3. 训练加载 `map_XXX.pt` 时会自动对齐起终点平面到地图记录值，但仍建议从源头保持预计算与训练配置一致。


## 8. 可视化与日志（按功能示例）

### 8.1 HTML 可视化行为

- A* 后端：显示 A* 路径相关层
- 势场后端：仅在势场作为顶层指导时，HTML 叠加势场切片

功能示例（势场 HTML）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps \
  --num_precomputed_maps 100
```

### 8.2 势场关键日志（当前版本）

说明：当前版本已移除 TensorBoard 中的 `Guidance/Potential_*` 相关展示项，避免面板冗余。

仍建议关注：

- Guidance/Dir_Align
- Guidance/Overspeed
- Guidance/Underspeed
- Guidance/Speed_Diff
- Guidance/Recovery_Speed
- Guidance/Field_Dir_Align
- Guidance/Applied_In_Current_Phase

功能示例：快速对比 A* 与势场（同迭代数）。

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub

# 势场实验
python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps \
  --num_precomputed_maps 100 \
  --num_iters 5000 \
  --exp_name potential_cmp

# A* 对照
python mmgj_transformer.py \
  --guidance_backend astar \
  --num_iters 5000 \
  --exp_name astar_cmp
```

### 8.3 单独可视化 precomputed_maps 的 pt 势场

脚本入口：`visualize_precomputed_potential.py`

用途：不启动训练，直接读取 `map_XXX.pt` 并输出 3D 交互可视化（Plotly HTML）：

- 三维势场点云（归一化 potential）
- 三维下降方向箭头（Cone）
- 三维占据栅格点（黑色散点）

功能示例 A（默认读取目录第 0 张图并弹窗显示）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python visualize_precomputed_potential.py \
  --map_dir /home/robot/transformer/precomputed_maps \
  --map_index 0
```

功能示例 B（指定高度切片与采样步长，箭头更稀疏）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python visualize_precomputed_potential.py \
  --map_dir /home/robot/transformer/precomputed_maps \
  --map_index 3 \
  --z_world 2.0 \
  --stride 5 \
  --arrow_stride 10 \
  --max_arrows 800
```

功能示例 C（保存为 HTML，不弹窗）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python visualize_precomputed_potential.py \
  --map_dir /home/robot/transformer/precomputed_maps \
  --map_index 3 \
  --z_world 2.0 \
  --stride 5 \
  --arrow_stride 10 \
  --max_arrows 800 \
  --save /home/robot/transformer/potential_map_003_z2.html
```

说明：

- 不传 `--z_world` 时，默认展示全高度范围的 3D 势场与箭头。
- 传 `--z_world` 时，仅展示该高度切片附近的场与箭头，便于局部分析。
- `--save` 仅支持 `.html` 输出。
- `--arrow_stride` 越大，箭头越稀疏。
- `--max_arrows` 用于限制箭头总数，避免 HTML 渲染过载。

### 8.4 最终可用的全面势场显示命令（推荐）

下面命令会生成“稀疏但清晰”的 3D 势场 HTML：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python visualize_precomputed_potential.py \
  --map_dir /home/robot/transformer/precomputed_maps \
  --map_index 3 \
  --z_world 2.0 \
  --stride 5 \
  --arrow_stride 12 \
  --max_arrows 600 \
  --save /home/robot/transformer/potential_map_003_z2_sparse.html
```


## 9. 常见问题排查

### 9.1 势场后端启动失败

检查项：

- precomputed_map_dir 是否存在
- 是否有 map_XXX.pt
- num_precomputed_maps 是否超过文件数


## 10. main_cuda.py 使用与开关总表

`main_cuda.py` 是一个偏轻量的训练入口，当前已经兼容 `env_multi.py`，并且在轨迹可视化上与 `mmgj_transformer.py` 保持同风格：

- TensorBoard 轨迹标签使用 `Trajectory/*`
- 周期性保存 3D 交互 HTML 轨迹
- 支持地图周期切换与仅无人机重置

### 10.1 最小可运行命令

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python main_cuda.py
```

### 10.2 输出目录与文件

训练启动后会自动创建目录：

- ../checkpoints/main_cuda_<exp_name>_<时间戳>/

其中主要内容：

- logs/: TensorBoard 日志
- videos/: 轨迹 HTML（例如 trajectory3d_iter_000250.html）
- checkpointXXXX.pth: 周期性模型权重

### 10.3 参数分组说明（按用途）

1. 训练规模与优化

- --resume: 从已有权重继续训练
- --batch_size (默认 64): 并行无人机数量
- --num_iters (默认 500000): 训练迭代次数
- --timesteps (默认 150): 每次迭代的仿真步数
- --lr (默认 1e-3): 学习率
- --grad_decay (默认 0.4): 环境相关衰减参数

2. 地图切换与终端刷新

- --maze_update_interval (默认 50): 每 N 次迭代重新生成地图；中间迭代只执行 reset_drone_only
- --terminal_log_interval (默认 500): 终端进度条刷新节奏（tqdm miniters）

3. 环境结构开关

- --single: 单机模式
- --gate: 启用 gate 场景
- --ground_voxels: 启用地面体素
- --scaffold: 启用脚手架场景
- --random_rotation: 初始随机旋转
- --yaw_drift: 加入偏航漂移扰动
- --no_odom: 模型输入不使用里程计速度

4. env_multi 对齐开关（成对互斥）

- --include_u_local_optimum / --no_include_u_local_optimum
  - 默认: 关闭（no）
  - 用途: 是否引入 U 型局部最优陷阱

- --compact_two_zone_map / --no_compact_two_zone_map
  - 默认: 关闭（no）
  - 用途: 是否启用两分区紧凑地图

- --wall_physical_feedback / --no_wall_physical_feedback
  - 默认: 关闭（no）
  - 用途: 是否启用墙体物理反馈

5. 观测与运动相关

- --coef_v (默认 1.0): 速度跟踪损失
- --coef_speed (默认 0.0): 速度项（legacy）
- --coef_v_pred (默认 2.0): 速度预测损失
- --coef_collide (默认 2.0): 碰撞相关损失
- --coef_obj_avoidance (默认 1.5): 障碍间距损失
- --coef_d_acc (默认 0.01): 控制加速度正则
- --coef_d_jerk (默认 0.001): jerk 正则
- --coef_d_snap (默认 0.0): snap 项（legacy）
- --coef_ground_affinity (默认 0.0): 地面亲和项（legacy）
- --coef_bias (默认 0.0): 偏置项（legacy）
- --speed_mtp (默认 1.0): 速度倍率
- --fov_x_half_tan (默认 0.53): 相机视场参数
- --cam_angle (默认 10): 相机角度

6. 实验命名

- --exp_name (默认 main_cuda): 影响输出目录命名

### 10.4 常用命令模板

模板 A：开启两分区紧凑地图 + 墙体反馈

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python main_cuda.py \
  --compact_two_zone_map \
  --maze_update_interval 20 \
  --exp_name compact_wall \
  --wall_physical_feedback 
```

模板 B：关闭里程计输入并加入偏航漂移（更难设置）

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python main_cuda.py \
  --no_odom \
  --yaw_drift \
  --terminal_log_interval 200 \
  --exp_name no_odom_drift
```

模板 C：从 checkpoint 继续训练

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub
python main_cuda.py \
  --resume ../checkpoints/xxx/checkpoint0009.pth \
  --exp_name resume_run
```

### 10.5 说明与注意事项

1. `main_cuda.py` 当前未接入 `guidance_backend / precomputed_map_dir` 这组势场顶层参数，若你需要势场后端切换，请使用 `mmgj_transformer.py`。
2. 当 `timesteps` 设得过小时，部分滑窗统计可能触发数值不稳定；建议先使用默认 `timesteps=150`。
3. 若只想快速看轨迹 HTML，可先把 `--num_iters` 设为 300~1000，便于快速产出可视化文件。

功能示例：检查目录内容。

```bash
conda activate DiffPhysDrone
ls ../precomputed_maps | head
```


### 9.2 guidance 看起来没生效

检查项：

- Guidance/Backend_Is_Potential 是否为 1
- Guidance/Applied_In_Current_Phase 是否在 LGN phase 为 1

说明：worker phase 仍以 proxy 为主，guidance 主要通过 LGN meta 路径作用。


### 9.3 HTML 没有势场层

检查项：

- guidance_backend 是否是 dijkstra_potential
- 当前轮次是否触发 HTML 保存
- 当前 map 数据是否包含有效 potential


## 10. 推荐最小可复现实验流程

1. 预构建 100 张图
2. 跑势场短程训练（2k~5k）
3. 跑 A* 同配置短程训练
4. 对比势场日志与 HTML

功能示例（一条龙）：

```bash
conda activate DiffPhysDrone
cd /home/robot/transformer/multi_pub

python precompute_potential_maps.py --num_maps 100 --save_dir ../precomputed_maps

python mmgj_transformer.py \
  --guidance_backend dijkstra_potential \
  --precomputed_map_dir ../precomputed_maps \
  --num_precomputed_maps 100 \
  --num_iters 3000 \
  --exp_name quick_potential
```
