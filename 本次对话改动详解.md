# 本次对话改动详解（含具体代码）

## 当前真实实验条件（先统一语义）

- Exploration 分支：已修复 `time_window=50` 与 `lgn_timesteps=40` 的硬冲突。
    - 现在 `exploration_time_window` 可配置（默认 50），并在运行时自动裁剪到 `rollout_len - 1`。
    - 因此 LGN phase 下 exploration 项不再因窗口越界而恒为零。
- 权重约束语义：当前训练链路已去掉 LGN 权重的 `clamp(0.01, 100.0)`。
    - LGN 前向输出 `raw`；训练实际使用 `effective_weights_seq = weights_seq_raw = weights_seq`。
    - 即：对有限值不做非负约束、不做和为 1 约束。

本文档按本次对话的时间顺序，详细记录已完成的改动，包含对应文件、关键行号与代码片段。

---

## 1. 打开诊断开关，解决“日志看不到”

### 1.1 修改点
- 文件：`mmgj_transformer.py`
- 行：`262`
- 作用：将诊断输出频率默认改为每次迭代都可见。

```python
parser.add_argument('--diag_interval', type=int, default=1,
                    help='Print detailed DIAG logs every N iterations (<=0 disables)')
```

### 1.2 修改点
- 文件：`mmgj_transformer.py`
- 行：`264`
- 作用：默认启用二阶诊断探针。

```python
parser.add_argument('--diag_second_order', default=True, action='store_true',
                    help='Enable heavy second-order diagnostic probes (can be noisy and slow)')
```

---

## 2. 修正二阶诊断口径：从 raw 分项探针改为 weighted 分项探针

问题背景：
- 原始分项损失本身不一定直接依赖 LGN 参数，导致 `second_order(worker_grad)->lgn` 里容易看到结构性 `None`。
- 解决方式是把探针目标改为 `effective_weights_seq[...,k] * loss_k`，保证探针路径真实经过 LGN 权重分支。

### 2.1 修改点
- 文件：`mmgj_transformer.py`
- 行：`2671` 起（二阶诊断块）

```python
if args.diag_second_order:
    # Probe weighted per-term proxy components so each branch truly depends on LGN weights.
    weighted_speed = (effective_weights_seq[:, :, 0] * loss_speed_n).mean()
    weighted_dir = (effective_weights_seq[:, :, 1] * loss_dir_n).mean()
    weighted_avoid = (effective_weights_seq[:, :, 2] * loss_avoid_n).mean()
    weighted_expl = (effective_weights_seq[:, :, 3] * loss_expl_n).mean()

    g_speed = _grad_or_none_tuple(weighted_speed, fast_param_values)
    g_dir = _grad_or_none_tuple(weighted_dir, fast_param_values)
    g_avoid = _grad_or_none_tuple(weighted_avoid, fast_param_values)
    g_expl = _grad_or_none_tuple(weighted_expl, fast_param_values)

    lgn_param_list = list(lgn.parameters())
    _diag_grad_tuple_to_params("speed(weighted) second_order(worker_grad)->lgn", g_speed, lgn_param_list, i)
    _diag_grad_tuple_to_params("direction(weighted) second_order(worker_grad)->lgn", g_dir, lgn_param_list, i)
    _diag_grad_tuple_to_params("avoidance(weighted) second_order(worker_grad)->lgn", g_avoid, lgn_param_list, i)
    _diag_grad_tuple_to_params("exploration(weighted) second_order(worker_grad)->lgn", g_expl, lgn_param_list, i)
```

### 2.2 修改点
- 文件：`mmgj_transformer.py`
- 行：`2696`
- 作用：`act_only` 探针也改为 weighted 形式。

```python
act_only_loss = (act_for_diag.pow(2).mean() if act_for_diag is not None else torch.tensor(0.0, device=device))
act_only_weighted = effective_weights_seq[:, :, 0].mean() * act_only_loss
...
_diag_grad_tuple_to_params("act_only(weighted) second_order(worker_grad)->lgn", g_act_only, lgn_param_list, i)
```

---

## 3. 稳定训练链路：缩放目标量级 + meta 不可用时 fallback 到 proxy

### 3.1 修改点
- 文件：`mmgj_transformer.py`
- 行：`437`
- 作用：新增/使用标量目标缩放，减少量级不平衡导致的不稳定。

```python
def scale_scalar_objective(x, target_mag=10.0, eps=1e-6):
```

### 3.2 修改点
- 文件：`mmgj_transformer.py`
- 行：`193`
- 作用：`lgn_proxy_aux_weight` 默认值为 `0.2`。

```python
parser.add_argument('--lgn_proxy_aux_weight', type=float, default=0.2,
                    help='Auxiliary proxy-loss weight for LGN update; also used as fallback update when meta gradients are unusable')
```

### 3.3 修改点
- 文件：`mmgj_transformer.py`
- 行：`2756-2761`
- 作用：LGN 更新时采用 `scaled_meta + w*scaled_proxy`；若 meta 梯度不可用则仅用 `w*scaled_proxy`。

```python
scaled_meta = scale_scalar_objective(meta_loss_unrolled)
scaled_proxy = scale_scalar_objective(proxy_loss)
if meta_grad_usable:
    lgn_total = scaled_meta + args.lgn_proxy_aux_weight * scaled_proxy
else:
    lgn_total = args.lgn_proxy_aux_weight * scaled_proxy
    lgn_used_proxy_fallback = 1.0
```

---

## 4. env fallback 动力学稳定性修复：acos 输入边界保护

### 4.1 修改点
- 文件：`env.py`
- 行：`83-84`
- 作用：防止 `acos` 在边界点产生无穷斜率，污染高阶梯度。

```python
# Keep acos input away from +/-1 to avoid infinite slope in backward.
cosv = torch.clamp(dot / (n1 * n2).clamp_min(1e-8), -1.0 + 1e-4, 1.0 - 1e-4)
av = torch.acos(cosv) / max(float(ctl_dt), 1e-6)
```

### 4.2 关联关系（为什么改 env.py 会影响多机环境）
- 文件：`env_multi.py`
- 行：`7`

```python
from env import Env as BaseEnv, run as differentiable_run, run_torch as differentiable_run_torch, safe_normalize, update_state_vec_torch
```

- 文件：`env_multi.py`
- 行：`605`

```python
dyn_fn = differentiable_run_torch if self.use_meta_fallback else differentiable_run
```

- 文件：`env_multi.py`
- 行：`645`

```python
self.R = update_state_vec_torch(self.R, self.act, v_pred, alpha, 2)
```

---

## 5. 构建与环境文档补充（CUDA 扩展相关）

### 5.1 修改点
- 文件：`src/README.md`
- 行：`26`

```bash
conda activate DiffPhysDrone
```

### 5.2 修改点
- 文件：`src/README.md`
- 行：`40`

```bash
PIP_NO_BUILD_ISOLATION=1 pip install ./src --no-build-isolation --no-use-pep517
```

### 5.3 修改点
- 文件：`src/README.md`
- 行：`50`

```bash
export LD_LIBRARY_PATH=$TORCH_LIB:$LD_LIBRARY_PATH
```

---

## 6. 测试脚本补充：验证 fallback 二阶可导、CUDA 路径高阶受限

### 6.1 修改点
- 文件：`src/test.py`
- 行：`11`

```python
from env import run as custom_cuda_run, run_torch, update_state_vec_torch
```

### 6.2 fallback 二阶探针
- 文件：`src/test.py`
- 行：`86` 起

```python
# --- Path-level higher-order probe ---
# Fallback path: should support second-order gradients.
...
second_grad = torch.autograd.grad(inner_grad.sum(), w, allow_unused=True)[0]
assert second_grad is not None
assert torch.isfinite(second_grad).all()
```

### 6.3 CUDA 路径探针
- 文件：`src/test.py`
- 行：`124` 起

```python
act_next_cuda, p_next_cuda, v_next_cuda, a_next_cuda = custom_cuda_run(...)
...
second_grad_cuda = None
if inner_grad_cuda is not None:
    try:
        second_grad_cuda = torch.autograd.grad(inner_grad_cuda.sum(), w2, allow_unused=True)[0]
    except RuntimeError:
        second_grad_cuda = None
assert second_grad_cuda is None
```

---

## 7. 本次最后一轮核心需求：移除权重归一化与非负约束，改为无约束直通

你最新要求是：
1) 不做 `weights_seq / sum(weights_seq)` 归一化；
2) 不对 LGN 原始输出及实际使用权重做任何形式限制。

### 7.1 训练侧：移除归一化，直接使用原始权重
- 文件：`mmgj_transformer.py`
- 行：`2459-2466`

```python
# 纯学习模式: 直接使用 LGN 原始输出，不做归一化/非负约束
weights_seq_raw = weights_seq
...
effective_weights_seq = weights_seq_raw
```

### 7.2 训练侧诊断命名同步
- 文件：`mmgj_transformer.py`
- 行：`2461`, `2504`

```python
print(f"[DIAG iter={i}] weights_seq_raw: requires_grad={weights_seq_raw.requires_grad}, ...")
...
_diag_tensor_finite("weights_seq_raw", weights_seq_raw, i)
```

### 7.3 训练侧统计从“Entropy”改为“Std”
- 文件：`mmgj_transformer.py`
- 行：`2818` 附近（统计逻辑）
- 行：`2859`, `3139`（记录项）

```python
weight_std = effective_weights_seq.std(dim=-1).mean()
...
'Weight_Stats/Std': weight_std,
...
writer.add_scalar('Weights_Snapshot/Std', weight_std, i + 1)
```

### 7.4 权重曲线标题改为无约束语义
- 文件：`mmgj_transformer.py`
- 行：`3130`

```python
ax.legend(); ax.set_title(f"Iter {i} Weights Profile (Per Step, Unconstrained)")
```

### 7.5 LGN 输出侧：直接输出 raw
- 文件：`LossGenNet_transformer.py`
- 行：`109`

```python
# 5. 生成权重（无约束输出，不做归一化）
raw = self.head(last_token)
weights = raw

### 7.6 训练循环中隐藏 clamp 的清理（与无约束语义对齐）
- 文件：`mmgj_transformer.py`
- 变更：删除 `current_weights = sanitize_tensor(...).clamp(0.01, 100.0)` 这一行。
- 说明：此前这行会把“实际使用权重”重新限制为非负且有下界，和“无约束”语义冲突。

### 7.7 Exploration 时间窗口冲突修复
- 文件：`mmgj_transformer.py`
- 变更：新增参数并做动态窗口裁剪。

```python
parser.add_argument('--exploration_time_window', type=int, default=50,
                    help='Look-back gap for exploration overlap loss; effective window is auto-clipped to rollout_len-1')
```

```python
actual_T = p_history.shape[0]
exploration_window = min(int(args.exploration_time_window), max(0, actual_T - 1))
loss_exploration_seq = compute_overlap_loss_per_step(
    p_history, sigma=1.0, time_window=exploration_window
).permute(1, 0)
```
```

当前版本结论：
- LGN 原始输出：无约束（可正可负，不投影、不归一化）。
- 训练实际使用权重：`effective_weights_seq = weights_seq_raw = weights_seq`，同样无约束。

---

## 8. 备注

- 目前 `LossGenNet_transformer.py` 中 `output_temperature`、`weight_floor` 仍保留在构造参数里，但前向输出已不再使用 `softplus + floor` 路径。
- 若需要，可进一步做一次“无效参数清理”提交，去掉未使用参数并同步训练参数入口，避免后续误解。
